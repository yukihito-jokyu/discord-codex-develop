const DISCORD_MAX_LENGTH = 2000;
const DEFAULT_TTL_MS = 24 * 60 * 60 * 1000; // 24h
const RATE_LIMIT_TTL_MS = 60 * 60 * 1000; // 1h

const THREAD_SESSION_TTL_S = 24 * 60 * 60; // 24h in seconds
const THREAD_COMPLETED_TTL_S = 60 * 60; // 1h in seconds
const PLAN_OUTPUT_MAX_BYTES = 10 * 1024; // 10KB

export {
  DEFAULT_TTL_MS,
  DISCORD_MAX_LENGTH,
  PLAN_OUTPUT_MAX_BYTES,
  RATE_LIMIT_TTL_MS,
  THREAD_COMPLETED_TTL_S,
  THREAD_SESSION_TTL_S,
};
